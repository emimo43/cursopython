# Ejercicio 1 ->
#print(((3 + 2) / (2 * 5)) ** 2) # Se usa el orden de jerarquia, se comienza por parentesis -> Esta es la primera forma

# Ahora con la forma de usar las variables
#calculo = ((3 +2) / (2 * 5)) ** 2 # Segunda forma de hacerlo
#print(calculo)

# Tercera forma de hacerlo

calculo = pow(5 , 2) # Aqui 5 se eleva al cuadrado, esto es el metodo pow eleva un valor
print(calculo)

calculo = pow(((3 + 2) / (5 * 2)), 2) # Hicimos la formula con el metodo pow
print(calculo)
